import React from 'react';

// The theme toggle is removed as the app is now permanently in dark mode.
// This component returns null to avoid breaking imports.
export const ThemeToggle: React.FC = () => {
  return null;
};